- name: Frentend setup
  hosts: all
  become: yes
  tasks:
    - name: Install Nginx
      ansible.builtin.dnf:
        name: nginx
        state: latest
    - name: start a Nginx
      ansible.builtin.systemd_service:
        state: started
        name: nginx